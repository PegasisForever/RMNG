/target
**/target/
node_modules/
frontend/build/
frontend/storybook-static/
# dev payload staging dir — its own .gitignore ignores everything but itself
crates/control-server/embedded-bin/*
!crates/control-server/embedded-bin/.gitignore
*.tmp.*

# Local test-run artifacts: state, uploads, and generated SSH host/fleet KEYS.
# The suite writes real private keys here; they must never reach a remote.
data/
