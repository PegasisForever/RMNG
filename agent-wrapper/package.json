{
  "name": "agent-wrapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small HTTP wrapper around the pi coding agent. Runs inside each RDP container on :4096; the control-server drives a persistent per-host chat through it. The agent runs on gpt-5.6-luna via the clone's pushed Codex token and controls the desktop via the computer-use MCP.",
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.85.0",
    "@earendil-works/pi-coding-agent": "^0.85.0",
    "@earendil-works/pi-server": "^0.85.0",
    "pi-mcp-adapter": "^2.32.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.7.0"
  }
}
